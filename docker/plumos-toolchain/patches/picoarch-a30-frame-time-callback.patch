--- a/core.c
+++ b/core.c
@@ -27,6 +27,7 @@ enum retro_pixel_format core_pixel_format = RETRO_PIXEL_FORMAT_0RGB1555;
 static char content_dir[MAX_PATH];
 static char core_assets_dir[MAX_PATH];
 static bool support_no_game;
+static struct retro_frame_time_callback frame_time_callback;
 static struct retro_disk_control_ext_callback disk_control_ext;
 
 static uint32_t buttons = 0;
@@ -191,6 +192,20 @@ bool state_allowed(void) {
 	return current_core.retro_serialize_size() > 0;
 }
 
+void core_run_frame_time_callback(void)
+{
+	retro_usec_t usec;
+
+	if (!frame_time_callback.callback)
+		return;
+	usec = frame_time_callback.reference;
+	if (usec <= 0 && frame_rate > 0.0)
+		usec = (retro_usec_t)(1000000.0 / frame_rate);
+	if (usec <= 0)
+		usec = 16667;
+	frame_time_callback.callback(usec);
+}
+
 void state_file_name(char *name, size_t size, int slot) {
 	char extension[5] = {0};
 
@@ -720,6 +734,13 @@ static bool pa_environment(unsigned cmd, void *data) {
 			*out = RETRO_LANGUAGE_JAPANESE;
 		break;
 	}
+	case RETRO_ENVIRONMENT_SET_FRAME_TIME_CALLBACK: { /* 21 */
+		if (data)
+			memcpy(&frame_time_callback, data, sizeof(frame_time_callback));
+		else
+			memset(&frame_time_callback, 0, sizeof(frame_time_callback));
+		break;
+	}
 	case RETRO_ENVIRONMENT_GET_VFS_INTERFACE: { /* 45 */
 		struct retro_vfs_interface_info *info = (struct retro_vfs_interface_info *)data;
 		if (!info || info->required_interface_version > 3)
--- a/core.h
+++ b/core.h
@@ -51,6 +51,7 @@ void sram_read(void);
 void sram_write(void);
 
 bool state_allowed(void);
+void core_run_frame_time_callback(void);
 void state_file_name(char *name, size_t size, int slot);
 int state_read(void);
 int state_write(void);
--- a/main.c
+++ b/main.c
@@ -659,8 +659,10 @@ int main(int argc, char **argv) {
 		count_fps();
 		adjust_audio();
 		core_poll_input();
-		if (!should_quit)
+		if (!should_quit) {
+			core_run_frame_time_callback();
 			current_core.retro_run();
+		}
 		if (!should_quit)
 			plat_video_flip();
 	} while (!should_quit);
