--- a/command.c
+++ b/command.c
@@ -898,6 +898,21 @@ bool command_load_savefiles(command_t *cmd, const char* arg)
    return ret;
 }
 
+bool command_audio_reinit(command_t *cmd, const char* arg)
+{
+   char reply[4];
+   bool ret;
+   size_t _len = strlcpy(reply, "OK", sizeof(reply));
+   reply[  _len] = '\n';
+   reply[++_len] = '\0';
+
+   ret = command_event(CMD_EVENT_AUDIO_REINIT, NULL);
+   if (!ret)
+      strlcpy(reply, "NO", sizeof(reply));
+   cmd->replier(cmd, reply, _len);
+   return ret;
+}
+
 #if defined(HAVE_CHEEVOS)
 bool command_read_ram(command_t *cmd, const char *arg)
 {
--- a/command.h
+++ b/command.h
@@ -435,6 +435,7 @@ bool command_seek_replay(command_t *cmd, const char *arg);
 bool command_save_savefiles(command_t *cmd, const char* arg);
 bool command_load_savefiles(command_t *cmd, const char* arg);
+bool command_audio_reinit(command_t *cmd, const char* arg);
 #ifdef HAVE_CHEEVOS
 bool command_read_ram(command_t *cmd, const char *arg);
 bool command_write_ram(command_t *cmd, const char *arg);
 #endif
@@ -464,6 +465,7 @@ static const struct cmd_action_map action_map[] = {
 
    { "SAVE_FILES", command_save_savefiles, "No argument"},
    { "LOAD_FILES", command_load_savefiles, "No argument"},
+   { "AUDIO_REINIT", command_audio_reinit, "No argument"},
 
    { "LOAD_CORE", command_load_core, "<core path>"},
 };
